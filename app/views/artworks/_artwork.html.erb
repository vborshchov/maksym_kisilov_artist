<% @artworks.each do |artwork| %>
  <%= cl_image_tag(artwork.picture, class: "protectMe") %> <br>
  "<%= artwork.name %>"<br>
  <%= artwork.dimension %><br>
  <%= artwork.material %><br>
  <%= artwork.category.name %><br>
<% end %>