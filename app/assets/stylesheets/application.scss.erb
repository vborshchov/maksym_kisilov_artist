/*
 * This is a manifest file that'll be compiled into application.css, which will include all the files
 * listed below.
 *
 * Any CSS and SCSS file within this directory, lib/assets/stylesheets, vendor/assets/stylesheets,
 * or any plugin's vendor/assets/stylesheets directory can be referenced here using a relative path.
 *
 * You're free to add application-wide styles to this file and they'll appear at the bottom of the
 * compiled file so the styles you add here take precedence over styles defined in any styles
 * defined in the other CSS/SCSS files in this directory. It is generally better to create a new
 * file per style scope.
 *
 *= require_tree .
 *= require_self
 *= require foundation_and_overrides
 *= require foundation-icons
 *= require photoswipe
 *= require slick
 *= require slick-theme
 */

@font-face {
  font-family: "EsqaderoRegular";
  src: url(<%= asset_url('EsqaderoRegular.eot') %>);
  src: url(<%= asset_url('EsqaderoRegular.eot?#iefix') %>) format("embedded-opentype"),
    url(<%= asset_url('EsqaderoRegular.woff') %>) format("woff"),
    url(<%= asset_url('EsqaderoRegular.ttf') %>) format("truetype");
  font-style: normal;
  font-weight: normal;
}

@font-face {
  font-family: "AdventureRegular";
  src: url(<%= asset_url('AdventureRegular.ttf') %>);
}

body {
  height: auto;
  color: white;
}

.hide-for-small-only {
  nav.top-bar {
    text-align:center;
  }
  section.top-bar-section {
    display: inline-block;
  }
  .lang-icon {
    position: absolute;
    top: 1rem;
    right: 0.8rem;
  }
}

blockquote {
  margin-left: 2rem;
}

h1.title {
  font-size: 4rem;
  margin: 1.5rem 0 0rem 0;
  text-align: center;
  font-family: "AdventureRegular";
}

.title a, .name a {
  color:white;
}

.name a {
  font-size: 1.5rem;
}

.title a:hover, .name a:hover {
  color:white;
}

.name>a {
  font-family: "AdventureRegular";
  float: left;
  padding: 1rem;
}

.empty {
  display:none;
}

.sortable {
   /* define how much padding you want in between your images */
  padding:1rem;

  img {
    /* ensures padding at the bottom of the image is correct */
    vertical-align:bottom;
    /*opacity: 0;*/
  }
}

h1 a {
  color: white;
  &:hover{
    color: white;
  }
}

.slick-slider {
  margin-top: 2rem;
}

.breadcrumb {
  line-height: 4rem;
  color: #aaaaaa;
  font-size: 0.9rem;
  &:hover {
    color:white;
  }
}

.breadcrumb.category {
  margin-left: 1rem;

  &:after{
    border: inset 5px;
    content: "";
    display: inline-block;
    border-color: rgba(255, 255, 255, 0.4) transparent transparent transparent;
    border-top-style: solid;
    margin: -2.5px 0 -2.5px 5px;
    box-sizing: inherit;
  }
}

.lang-icon {
  margin-right: 0.8rem;
  img {
    width: 1.6rem;
    height: 1.2rem;
    margin: 0 0.1rem 0.1rem 0;
  }
}

.protect {
  margin: 0 auto;
}

.my-gallery figcaption {
  display: none;
}

fieldset legend {
  background-color: #3C4045;
}

.switch {
  margin: 0.9rem 1.25rem 0 0;
  label {
    /*margin: 0.2rem 0 0 0;*/
    background: #939393 none repeat scroll 0% 0%;
  }
}

.has-tip {
  border-bottom: none;
}

.cd-top {
  display: inline-block;
  height: 40px;
  width: 40px;
  position: fixed;
  bottom: 40px;
  right: 10px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.05);
  /* image replacement properties */
  overflow: hidden;
  text-indent: 100%;
  white-space: nowrap;
  background: rgba(51, 51, 51, 0.3) url("<%= asset_url('cd-top-arrow.png') %>") no-repeat center 50%;
  visibility: hidden;
  opacity: 0;
  -webkit-transition: opacity .3s 0s, visibility 0s .3s;
  -moz-transition: opacity .3s 0s, visibility 0s .3s;
  transition: opacity .3s 0s, visibility 0s .3s;

  &.cd-is-visible, &.cd-fade-out, .no-touch &:hover {
    -webkit-transition: opacity .3s 0s, visibility 0s 0s;
    -moz-transition: opacity .3s 0s, visibility 0s 0s;
    transition: opacity .3s 0s, visibility 0s 0s;
  }

  &.cd-is-visible {
    /* the button becomes visible */
    visibility: visible;
    opacity: 1;
  }

  &.cd-fade-out {
    /* if the user keeps scrolling down, the button is out of focus and becomes less visible */
    opacity: .5;
  }

  .no-touch &:hover {
    background-color: rgba(129, 165, 196, 0.3);
    opacity: 1;
  }
}

@media only screen and (min-width: 768px) {
  .cd-top {
    right: 20px;
    bottom: 20px;
  }
}

@media only screen and (min-width: 1024px) {
  .cd-top {
    height: 60px;
    width: 60px;
    right: 30px;
    bottom: 30px;
  }
}

footer {
  background-color: #333333;
  text-align: center;
  font-size: 1.2rem;
  padding: 1rem;

  p {
    &.slogan {
      color: #8d9093;
      font-size: 1.8rem;
    }
    &.copywrite {
      color: #8f9296;
      font-size: 0.65rem;
      margin: 0;
    }
  }
  .links {
    a {
      white-space: nowrap;
      list-style: none;
      font-size: 0.8125rem;
      color: #aaaaaa;
      margin: 0;

      &:hover {
        color: white;
      }

      &::after {
        content: "•";
        padding: 0 0.4rem 0 0.8rem;
      }

      &:last-child::after {
        content: "";
        padding: 0 0.4rem 0 0.8rem;
      }
    }
  }

  .social-links li {
    background: #ACAFB3;
    padding: 0 .845rem;
    border: 3px solid #333333;
    border-radius: 100px;
    list-style: none;
    margin-right: 1rem;
    font-size: 2rem;
    display: inline-block;

    &[class="facebook"]:hover {
      background: #dddddd;
    }

    &[class="pinterest"]:hover {
      background: #dddddd;
    }

    &[class="saatchiart"]:hover {
      background: #dddddd;
    }

    &[class="saatchiart"] img {
      width: 1.5rem;
      height: 1.5rem;
    }
  }
}

.facebook i {
  color: #46629C;
}

.pinterest i {
  color: #A90000;
}


nav i, .links i {
  margin-right: 5px;
}

.lang-icon a {
  float: right;
  margin-right: 0.5rem;
}

.small {
  float: right;
  margin: 0.75rem 3rem 1rem 0.5rem;
  padding: 0;

  a {
    float: none;
  }
}

.title-area .logo-img {
  margin: 0.3rem 1rem;
}

.logo-img:hover {
  fill: #111;
}

.pager {
  line-height: 3rem;
  font-size: 2rem;
  margin-right: 1rem;

  li {
    list-style: none;
  }
}

.f-dropdown {
  li {
    list-style-type: none;
    a {
      color: #aaaaaa;
      &:hover {
        color: white;
      }
    }
  }
  &:focus {
    outline-width: 0px;
  }
}

::-webkit-input-placeholder {
   color: #333333;
}

:-moz-placeholder { /* Firefox 18- */
   color: #333333;
}

::-moz-placeholder {  /* Firefox 19+ */
   color: #333333;
}

:-ms-input-placeholder {
   color: #333333;
}


//Styles for gallery tree
.node rect {
  cursor: pointer;
  fill: #fff;
  fill-opacity: .5;
  stroke: #3182bd;
  stroke-width: 1.5px;
}

.node text {
  font: 10px sans-serif;
  pointer-events: none;
}

path.link {
  fill: none;
  stroke: #9ecae1;
  stroke-width: 1.5px;
}